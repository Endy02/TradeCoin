/:
  controller: default
  action: default

/test/yves:
  controller: default
  action: default

/ajout-utilisateur:
  controller: "user"
  action: "add"

/se-connecter:
  controller: "user"
  action: "login"

/s-inscrire:
  controller: "user"
  action: "register"

/mot-de-passe-oublie:
  controller: "user"
  action: "forgotPwd"

/tableau-de-bord:
  controller: "user"
  action: "dashboardBack"

/pages:
  controller: "user"
  action: "allPage"

/cours:
  controller: "user"
  action: "cours"

/apparence:
  controller: "user"
  action: "apparence"

/utilisateur:
  controller: "user"
  action: "default"

/categories:
  controller: "user"
  action: "categories"

/tradeline:
  controller: "user"
  action: "tradeLine"